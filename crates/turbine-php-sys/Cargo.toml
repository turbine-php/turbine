[package]
name = "turbine-php-sys"
description = "Raw FFI bindings to libphp (PHP embed SAPI)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
build = "build.rs"

[dependencies]
libc = { workspace = true }

[build-dependencies]
cc = "1"
